import type { RunResult } from "../types.js"; const pct=(n:number|null)=>n===null?"n/a":`${(n*100).toFixed(1)}%`;
export function report(run:RunResult){const m=run.metrics.missing,c=run.metrics.control;const bad=run.results.filter(r=>(r.ground_truth==="requires_missing"&&!r.whether_missing_was_called)||(r.ground_truth==="solvable_without_missing"&&(r.whether_missing_was_called||!r.expected_tool||!r.selected_tools.includes(r.expected_tool))));return [`MISSING EXPERIMENT 0`,`====================`,`Run type: ${run.run_type}`,`Cases: ${m.cases}`,`Requires MISSING: ${m.requires_missing}`,`Solvable normally: ${m.solvable_without_missing}`,``,`Fallback selection rate:    ${pct(m.fallback_selection_rate)}`,`False positive rate:         ${pct(m.false_positive_rate)}`,`No-fallback rate:            ${pct(m.no_fallback_rate)}`,`Correct normal tool rate:    ${pct(m.correct_normal_tool_rate)}`,`First-tool correct rate:     ${pct(m.first_tool_correct_rate)}`,`MISSING first-call rate:     ${pct(m.missing_first_call_rate)}`,`Precision MISSING:           ${pct(m.precision_missing)}`,``,`Control comparison: fallback ${pct(c.fallback_selection_rate)}, no-fallback ${pct(c.no_fallback_rate)}, correct normal tool ${pct(c.correct_normal_tool_rate)}`,``,`Misclassified cases (${bad.length}):`,...bad.map(r=>`- ${r.case_id} [${r.condition}]: selected=${r.selected_tools.join(",")||"none"}`)].join("\n");}
