import type {Config} from "./config/index.js";
import type {DocEvidence,MicroContract} from "./experiment3wCore.js";
import {FOUR_A_CASES} from "./experiment4aCore.js";
import type {Provider4A} from "./experiment4aContract.js";
const BY_ID=Object.fromEntries(FOUR_A_CASES.map(x=>[x.case_id,x]));
function evidencePayload(evidence:DocEvidence[]){let used=0;const max=42000;return evidence.filter(e=>e.state==="ok").map(e=>{const left=Math.max(0,max-used),text=e.text.slice(0,Math.min(12000,left));used+=text.length;return {evidence_id:e.evidence_id,url:e.resolved_url,content_type:e.content_type,text};}).filter(x=>x.text.length>0);}
export async function synthesize4a(config:Config,p:Provider4A,evidence:DocEvidence[],phase:"initial"|"repair",previousError?:string){const ec=BY_ID[p.case_id];const system=["You are MISSING's documentation-to-contract compiler.","Use ONLY supplied documentation evidence.","If evidence does not literally support an anonymous HTTPS GET operation satisfying every required output, return decision REJECT.","Never invent host, path, parameter, output field, credential, header, endpoint, transformation, or provider.","For COMPILE return exactly: case_id, provider_candidate_id, decision, method, base_url, path_template, path_bindings, query_bindings, output_paths, evidence_ids, reason.","Input references use $input.<name>.","output_paths are canonical-output keys mapped to direct dotted JSON response paths; numeric array indexes are allowed only when grounded by evidence.","No JavaScript, expressions, functions, loops, LOOKUP or FIND.","method must be GET; base_url must be an HTTPS origin literally present in cited evidence."].join(" ");const payload={case:{case_id:p.case_id,input_names:Object.keys(ec.build),required_output_leaf_names:ec.required,build_input:ec.build,replay_input:ec.replay},provider:{candidate_id:p.candidate_id,name:p.name},phase,previous_validation_error:previousError??null,evidence:evidencePayload(evidence)};const t0=Date.now();const r=await fetch(`${config.baseUrl.replace(/\/$/,"")}/chat/completions`,{method:"POST",headers:{authorization:`Bearer ${config.apiKey}`,"content-type":"application/json"},body:JSON.stringify({model:config.model,messages:[{role:"system",content:system},{role:"user",content:JSON.stringify(payload)}],response_format:{type:"json_object"}})});const latency_ms=Date.now()-t0,text=await r.text();let body:any=null;try{body=JSON.parse(text);}catch{}const msg=body?.choices?.[0]?.message,raw=msg?.content??null;let parsed:MicroContract|null=null,parse_error:string|null=null;if(typeof raw==="string"){try{parsed=JSON.parse(raw);}catch(e){parse_error=String(e);}}return {phase,http_status:r.status,http_ok:r.ok,refusal:msg?.refusal??null,raw_content:raw,parsed_json:parsed,parse_error,usage:body?.usage??null,latency_ms};}
