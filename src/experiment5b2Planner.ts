import type {Config} from "./config/index.js";
import type {RecoveryLedger} from "./experiment3yrCore.js";
import type {DocEvidence} from "./experiment3wCore.js";
import {FOUR_A_CASES} from "./experiment4aCore.js";
import type {Provider4A} from "./experiment4aContract.js";
import type {P1RequestHypothesis} from "./experiment4ap1Model.js";
import {prepareRequestGraph4Q} from "./experiment4qRequest.js";
import {probeHypothesis4R,probeCacheMetrics4R} from "./experiment4rProbe.js";
import {deterministicPartialProjection4S,groundingStats4S,materialize4S} from "./experiment4sCore.js";
import {ingestLinkedContracts4S} from "./experiment4sLinkedFetch.js";
import {freezeRequestProof4U} from "./experiment4uNativeOperation.js";
import {semanticErrorEnvelope4V,responseUsable4V} from "./experiment4vResponse.js";
import {buildNativeHypotheses5B} from "./experiment5bNativeOperation.js";
import {applyResponseRoleWitness5B,combineUsage5B,resolveResponseRoles5B} from "./experiment5bResponse.js";
import {deriveSemanticIdentityWitness5B2,sanitizeProjectionInputs5B2} from "./experiment5b2Semantics.js";
import {sha4u} from "./experiment4uOpenApi.js";

const BY_ID=Object.fromEntries(FOUR_A_CASES.map(c=>[c.case_id,c]));
function defaults(){return {crossGraphCandidates5b2:0,crossGraphDeduplicatedCandidates5b2:0,crossGraphAlreadyAttempted5b2:0,crossGraphAdditionalProbeCalls5b2:0,crossGraphSecondHypothesisAttempts5b2:0,crossGraphRecoveredRecipes5b2:0,crossGraphThirdProbeRejects5b2:0,strictInheritedRepairAttempts5b2:0,strictInheritedRepairSuccesses5b2:0,strictInheritedRepairResponseResolverCalls5b2:0,semanticWitnessCandidates5b2:0,semanticWitnessCompatible5b2:0,semanticWitnessRoleRejects5b2:0,semanticWitnessPaginationRejects5b2:0,semanticWitnessDescriptiveTextRejects5b2:0,semanticWitnessExactSelected5b2:0,semanticWitnessNumericExactSelected5b2:0,semanticWitnessCompositeSelected5b2:0,semanticWitnessContainmentSelected5b2:0,semanticWitnessStrongerCandidateSuperseded5b2:0,semanticIdentityValidationSuccesses5b2:0,semanticIdentityRejects5b2:0,projectionInputBindingsChecked5b2:0,projectionInputBindingsCompatible5b2:0,projectionInputRoleRejects5b2:0,projectionHumanNameFromCodeRejects5b2:0,responseGroundedCandidateFields5b:0,responseGroundedResolverCalls5b:0,responseGroundedMappingsAccepted5b:0,responseGroundedNoValidSource5b:0,responseGroundedInventedPathRejects5b:0,responseGroundedAmbiguousRejects5b:0,responseGroundedValueGenerationRejects5b:0,outputCoverageCandidates5b:0,outputRolesDocumented5b:0,outputRolesUnsupported5b:0,operationsRankedByOutputCoverage5b:0,operationsRejectedNoOutputSupport5b:0,requestFirstProbeCalls5b:0,requestFirstStructured2xx5b:0,requestFirstTransport2xx5b:0,requestFirstUnusable2xx5b:0,requestFirstConfirmedRecipes5b:0,responseGroundedConfirmedRecipes5b:0,frozenRequestProofs4u:0,frozenInheritedRequestProofs4u:0,frozenNativeRequestProofs4u:0}}
function add(target:any,src:any){for(const [k,v] of Object.entries(src||{}))if(typeof v==="number")target[k]=(target[k]||0)+v;return target}
function shape(h:P1RequestHypothesis){return sha4u({origin:h.origin,path:h.full_path,input_bindings:h.input_bindings,literal_bindings:h.literal_bindings})}
function mergeMetrics(a:any,b:any){return {...a,...b}}
function trace(h:any){return String(h?.proof_type||"").startsWith("4n_executed_trace_")}
function documentedRole(h:any){return String(h?.proof_type||"").startsWith("4n_executed_trace_documented_role_")}
function delta(a:any,b:any,k:string){return Number(b?.[k]||0)-Number(a?.[k]||0)}

export async function synthesize5b2(config:Config,provider:Provider4A,evidence:DocEvidence[],ledger:RecoveryLedger){const c=BY_ID[provider.case_id];if(!c)throw new Error(`5b2_unknown_case:${provider.case_id}`);const metrics:any=defaults(),usageHolder:{usage:any}={usage:null};const linked=await ingestLinkedContracts4S(evidence,provider,ledger),allEvidence=[...evidence,...linked.evidence],request=await prepareRequestGraph4Q(allEvidence,provider.case_id,provider.candidate_id,ledger),native=await buildNativeHypotheses5B(allEvidence,provider,ledger);add(metrics,linked.metrics);add(metrics,native.metrics);const nativeProofById=new Map((native.proofs||[]).map((p:any)=>[p.hypothesis_id,p]));const primary=(request.probe_packet||[])[0] as P1RequestHypothesis|undefined,rest=[...(request.probe_packet||[]).slice(primary?1:0),...(native.hypotheses||[])];const ordered:P1RequestHypothesis[]=[];const seen=new Set<string>();if(primary){ordered.push(primary);seen.add(shape(primary))}rest.sort((a:any,b:any)=>Number(b.score||0)-Number(a.score||0)||String(a.id).localeCompare(String(b.id)));for(const h of rest){const fp=shape(h);if(seen.has(fp))continue;seen.add(fp);ordered.push(h)}metrics.crossGraphCandidates5b2=(request.probe_packet||[]).length+(native.hypotheses||[]).length;metrics.crossGraphDeduplicatedCandidates5b2=ordered.length;if(ordered.length>2)metrics.crossGraphThirdProbeRejects5b2=ordered.length-2;const beam=ordered.slice(0,2),cacheBefore=probeCacheMetrics4R(),rows:any[]=[];let selected:any=null;
for(let i=0;i<beam.length;i++){const h=beam[i],isPrimary=!!primary&&shape(h)===shape(primary),isNative=nativeProofById.has(h.id);if(i===1){metrics.crossGraphAdditionalProbeCalls5b2++;metrics.crossGraphSecondHypothesisAttempts5b2++}metrics.requestFirstProbeCalls5b++;const row:any=await probeHypothesis4R(h,c.build,ledger);rows.push(row);const transport=row.proof.record.disposition==="success",structured=transport&&!!row.structured_format;metrics.requestFirstTransport2xx5b+=transport?1:0;metrics.requestFirstStructured2xx5b+=structured?1:0;if(!structured)continue;const ground=groundingStats4S(row.proof,c.required),errorEnvelope=semanticErrorEnvelope4V(row.decoded_body,ground.semanticGroundingAccepted);if(errorEnvelope){metrics.requestFirstUnusable2xx5b++;continue}const rawD=deterministicPartialProjection4S(row.proof,row.relational_summary,c.input_names,c.required),san=sanitizeProjectionInputs5B2(rawD.projection);add(metrics,san.metrics);let projection:any=san.projection,locked=rawD.locked.filter((x:string)=>!san.rejected.includes(x)),unresolved=[...new Set([...rawD.unresolved,...san.rejected])],responseWitness:any=null;const repairAttempt=san.rejected.length>0;if(repairAttempt)metrics.strictInheritedRepairAttempts5b2++;if(unresolved.length){const proofMeta:any=nativeProofById.get(h.id),resolved=await resolveResponseRoles5B(config,{intent:c.intent,root:proofMeta?.spec_root||{},op:proofMeta?.operation||{},proof:row.proof,unresolved});add(metrics,resolved.metrics);if(repairAttempt)metrics.strictInheritedRepairResponseResolverCalls5b2+=Number(resolved.metrics?.responseGroundedResolverCalls5b||0);usageHolder.usage=combineUsage5B(usageHolder.usage,resolved.usage);responseWitness=resolved.witness;if(responseWitness?.mappings?.length){projection=applyResponseRoleWitness5B(projection,responseWitness);const mapped=new Set<string>(responseWitness.mappings.map((m:any)=>String(m.output_role)));locked=[...locked,...mapped];unresolved=unresolved.filter((x:string)=>!mapped.has(x))}}if(unresolved.length){metrics.requestFirstUnusable2xx5b++;continue}const semantic=deriveSemanticIdentityWitness5B2({inputNames:c.input_names,input:c.build,projection,proof:row.proof});add(metrics,semantic.metrics);const usable=responseUsable4V({transport2xx:transport,structured,body:row.decoded_body,groundedRequired:ground.semanticGroundingAccepted,schemaWitnessAvailable:unresolved.length===0}).usable&&semantic.ok;if(!usable){metrics.requestFirstUnusable2xx5b++;continue}if(repairAttempt)metrics.strictInheritedRepairSuccesses5b2++;const materialized=materialize4S(provider.case_id,provider.candidate_id,row.proof,projection,row.relational_summary);materialized.raw.reason=`5B2_SEMANTIC_IDENTITY_BEAM:${h.id}`;materialized.raw.frozen_request_proof_4u=freezeRequestProof4U(h);materialized.raw.response_role_witness_5b=responseWitness;materialized.raw.semantic_identity_witness_5b2=semantic.witness;materialized.raw.input_output_compatibility_fingerprint_5b2=sha4u(Object.entries<any>(projection).filter(([,e])=>e?.op==="INPUT"));materialized.raw.cross_graph_rank_5b2=i+1;materialized.raw.cross_graph_recovered_5b2=i===1;if(i===1)metrics.crossGraphRecoveredRecipes5b2++;metrics.requestFirstConfirmedRecipes5b++;if(responseWitness?.mappings?.length)metrics.responseGroundedConfirmedRecipes5b++;metrics.frozenRequestProofs4u++;if(isNative)metrics.frozenNativeRequestProofs4u++;else metrics.frozenInheritedRequestProofs4u++;selected={row,h,isPrimary,isNative,materialized,responseWitness,semantic,locked,rawD};break}
const cacheAfter=probeCacheMetrics4R();const probeMetrics={schemaProbeCalls:rows.length,schemaProbe2xxJson:rows.filter(r=>r.proof.record.disposition==="success"&&r.structured_format==="json").length,schemaProbe2xxStructured:rows.filter(r=>r.proof.record.disposition==="success").length,schemaProbeRejected:rows.filter(r=>r.proof.record.disposition!=="success").length,schemaProbeBytes:rows.filter(r=>r.proof.record.disposition==="success").reduce((n:number,r:any)=>n+Number(r.proof.record.bytes||0),0),probeCacheHits:delta(cacheBefore,cacheAfter,"probeCacheHits"),probeNetworkFetches:delta(cacheBefore,cacheAfter,"probeNetworkFetches"),duplicateProbeNetworkFetches:delta(cacheBefore,cacheAfter,"duplicateProbeNetworkFetches")};const common={validation_evidence:allEvidence,linked_contract_evidence_ids:linked.linked_evidence_ids,request_inventory_count:request.hypotheses.length,alignment_metrics:request.metrics,operation_semantics_fingerprint:request.semantics.fingerprint,request_graph_fingerprint:request.graph_fingerprint,request_hypotheses:beam,probe_records:rows.filter(r=>r.proof.record.disposition==="success").map(r=>r.proof.record),cross_graph_beam_5b2:beam.map((h,i)=>({rank:i+1,hypothesis_id:h.id,request_fingerprint:shape(h),native:nativeProofById.has(h.id),score:h.score})),...probeMetrics,...metrics};if(!selected)return {http_status:0,http_ok:true,parsed_json:{case_id:provider.case_id,provider_candidate_id:provider.candidate_id,decision:"REJECT",reason:"5B2_BEAM_EXHAUSTED"},parse_error:null,usage:usageHolder.usage,latency_ms:0,probe_proof:null,planner_skipped:true,deterministic_mapping:false,row_lookup_selected:false,identity_projection_selected:false,compositional_locked_bindings:0,exact_relational_label_selections:0,structured_format:null,...common};const {row,h,isNative,materialized,responseWitness,semantic,locked,rawD}=selected;return {http_status:0,http_ok:true,parsed_json:materialized.raw,parse_error:null,usage:usageHolder.usage,latency_ms:0,probe_proof:row.proof,operation_proof:materialized.proof,planner_skipped:true,deterministic_mapping:true,row_lookup_selected:rawD.rowLookups>0,identity_projection_selected:rawD.identityInputs>0,selected_from_trace:trace(h),selected_from_documented_role:documentedRole(h),selected_from_linked_contract:(h.evidence_ids||[]).some((id:string)=>linked.linked_evidence_ids.includes(id)),selected_from_native_contract:isNative,compositional_locked_bindings:locked.length,exact_relational_label_selections:rawD.exactRelationalLabels,structured_format:row.structured_format,response_role_witness_5b:responseWitness,semantic_identity_witness_5b2:semantic.witness,cross_graph_recovered_5b2:materialized.raw.cross_graph_recovered_5b2,...common}}
