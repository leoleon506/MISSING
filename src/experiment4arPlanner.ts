import type {Config} from "./config/index.js";
import {FOUR_A_CASES} from "./experiment4aCore.js";
import type {Provider4A} from "./experiment4aContract.js";
import {selectEvidenceWindows} from "./experiment4arCore.js";
import type {DocEvidence} from "./experiment3wCore.js";

const BY_ID=Object.fromEntries(FOUR_A_CASES.map(x=>[x.case_id,x]));
export async function synthesize4ar(config:Config,p:Provider4A,evidence:DocEvidence[],phase:"initial"|"repair",previousError?:string){const ec=BY_ID[p.case_id];const system=["You are MISSING's evidence-grounded typed API compiler.","Use ONLY supplied evidence.","If evidence cannot support an anonymous HTTPS GET satisfying all required outputs, return REJECT.","Never invent provider, host, path, parameter, field, credential, header, transformation, or endpoint.","For COMPILE return exactly: case_id, provider_candidate_id, decision, method, base_url, path_template, path_bindings, query_bindings, projection, evidence_ids, reason.","path_template uses {provider_parameter} placeholders only; never embed $input.x in path_template and never use :id syntax.","path_bindings/query_bindings values may reference only $input.<name> or literal values explicitly documented in evidence.","projection must map every required canonical output to one of the existing 3X expressions: FIELD {path}; INPUT {name}; LOOKUP {map_path,key:{op:INPUT|LITERAL,...},value_path}; FIND {array_path,where_path,equals:{op:INPUT|LITERAL,...},value_path}.","Paths use dotted notation with numeric indices such as items.0.name; never JSONPath $, bracket indexes, or wildcard *.","Use FIND when an array must be searched by an input value; use LOOKUP when an object/map is keyed by an input value.","method must be GET; base_url must be an HTTPS origin literally present in cited evidence."].join(" ");const payload={case:{case_id:p.case_id,input_names:Object.keys(ec.build),required_outputs:ec.required,build_input:ec.build,replay_input:ec.replay},provider:{candidate_id:p.candidate_id,name:p.name},phase,previous_validation_error:previousError??null,evidence:selectEvidenceWindows(evidence,ec.required)};const t0=Date.now();const r=await fetch(`${config.baseUrl.replace(/\/$/,"")}/chat/completions`,{method:"POST",headers:{authorization:`Bearer ${config.apiKey}`,"content-type":"application/json"},body:JSON.stringify({model:config.model,messages:[{role:"system",content:system},{role:"user",content:JSON.stringify(payload)}],response_format:{type:"json_object"}})});const latency_ms=Date.now()-t0,text=await r.text();let body:any=null;try{body=JSON.parse(text);}catch{}const msg=body?.choices?.[0]?.message,raw=msg?.content??null;let parsed:any=null,parse_error:string|null=null;if(typeof raw==="string"){try{parsed=JSON.parse(raw);}catch(e){parse_error=String(e);}}return {phase,http_status:r.status,http_ok:r.ok,refusal:msg?.refusal??null,raw_content:raw,parsed_json:parsed,parse_error,usage:body?.usage??null,latency_ms};}
