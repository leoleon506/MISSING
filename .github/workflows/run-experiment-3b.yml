name: Run MISSING Experiment 3B

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  experiment-3b:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Run Experiment 3B Autonomous Primitive Discovery
        run: npm run benchmark:3b

      - name: Upload Experiment 3B results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: missing-experiment-3b-results
          path: results/experiment-3b/*.json
          if-no-files-found: error
          retention-days: 30
