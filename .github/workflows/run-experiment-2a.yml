name: Run MISSING Experiment 2A

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  experiment-2a:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Run Experiment 2A Autonomous Capability Forge
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5.6-luna
          OPENAI_BASE_URL: https://api.openai.com/v1
          AGENT_PROVIDER: openai
        run: |
          test -n "$OPENAI_API_KEY"
          npm run benchmark:2a

      - name: Upload Experiment 2A results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: missing-experiment-2a-results
          path: results/experiment-2a/*.json
          if-no-files-found: error
          retention-days: 30
